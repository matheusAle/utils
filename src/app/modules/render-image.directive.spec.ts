import { RenderImageDirective } from './render-image.directive';

describe('RenderImageDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderImageDirective();
    expect(directive).toBeTruthy();
  });
});
